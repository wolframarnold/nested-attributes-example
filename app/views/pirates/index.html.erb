<h1>Listing pirates</h1>

<table>
  <tr>
    <th>Catchphrase</th><th>Ship</th>
  </tr>

<% @pirates.each do |pirate| %>
  <tr>
    <td><%=h pirate.catchphrase %></td>
    <td><%=h pirate.ship.try(:name) %></td>
    <td><%= link_to 'Show', pirate %></td>
    <td><%= link_to 'Edit', edit_pirate_path(pirate) %></td>
    <td><%= link_to 'Destroy', pirate, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New pirate', new_pirate_path %>